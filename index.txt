<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ana & Mario - ¡Nos casamos!</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Cinzel:wght@500;700&family=Marcellus&family=Pinyon+Script&display=swap');

        :root {
            --gold: #c5a059;
            --dark-gold: #9a763e;
            --charcoal: #2c2a29;
            --parchment: #f4efe2;
        }

        body {
            font-family: 'Marcellus', serif;
            background-color: var(--parchment);
            background-image: url('https://www.transparenttextures.com/patterns/parchment.png');
            color: var(--charcoal);
            margin: 0;
            padding: 0;
            text-align: center;
        }

        .header {
            padding: 60px 20px;
            border-bottom: 2px double var(--gold);
            max-width: 800px;
            margin: 0 auto;
        }

        h1 {
            font-family: 'Cinzel', serif;
            font-size: 3rem;
            margin: 0;
            letter-spacing: 4px;
            color: var(--charcoal);
            text-transform: lowercase;
        }
        h1::first-letter, h1 .and::first-letter, h1 .name2::first-letter {
            text-transform: uppercase;
        }

        .subtitle {
            font-size: 1.5rem;
            letter-spacing: 2px;
            margin-top: 10px;
            color: var(--dark-gold);
        }

        .quote-container {
            font-family: 'Marcellus', serif;
            font-style: italic;
            max-width: 600px;
            margin: 40px auto;
            padding: 20px;
            border-left: 3px solid var(--gold);
            border-right: 3px solid var(--gold);
            font-size: 1.2rem;
            line-height: 1.6;
        }

        .section {
            padding: 50px 20px;
            max-width: 800px;
            margin: 0 auto;
        }

        h2 {
            font-family: 'Cinzel', serif;
            color: var(--dark-gold);
            font-size: 1.8rem;
            letter-spacing: 2px;
            margin-bottom: 30px;
        }

        /* Contador */
        #countdown {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin: 30px auto;
        }

        .time-box {
            background: rgba(255, 255, 255, 0.5);
            padding: 15px 25px;
            border-radius: 4px;
            border: 1px solid var(--gold);
            min-width: 80px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.05);
        }

        .time-number {
            font-size: 2.2rem;
            font-weight: bold;
            font-family: 'Cinzel', serif;
            color: var(--charcoal);
        }

        .time-label {
            font-size: 0.8rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: var(--dark-gold);
            margin-top: 5px;
        }

        .card {
            background: rgba(255, 255, 255, 0.4);
            border: 1px solid var(--gold);
            padding: 30px;
            margin: 20px auto;
            max-width: 500px;
            border-radius: 2px;
        }

        .btn {
            display: inline-block;
            background-color: var(--charcoal);
            color: var(--parchment);
            padding: 12px 30px;
            text-decoration: none;
            font-family: 'Cinzel', serif;
            letter-spacing: 2px;
            font-size: 0.9rem;
            border: 1px solid var(--gold);
            transition: all 0.3s ease;
            margin-top: 20px;
            cursor: pointer;
        }

        .btn:hover {
            background-color: var(--dark-gold);
            color: white;
        }

        .footer {
            padding: 40px 20px;
            font-size: 0.9rem;
            color: #777;
            border-top: 1px solid rgba(197, 160, 89, 0.3);
            max-width: 800px;
            margin: 40px auto 0 auto;
        }
    </style>
</head>
<body>

    <div class="header">
        <h1>ana & mario</h1>
        <div class="subtitle">¡Nos Casamos!</div>
    </div>

    <div class="quote-container">
        "Prefiero compartir una vida contigo que enfrentarme a todas las edades de este mundo sola."
    </div>

    <div class="section">
        <h2>La Cuenta Atrás</h2>
        <div id="countdown">
            <div class="time-box"><div class="time-number" id="days">00</div><div class="time-label">Días</div></div>
            <div class="time-box"><div class="time-number" id="hours">00</div><div class="time-label">Horas</div></div>
            <div class="time-box"><div class="time-number" id="minutes">00</div><div class="time-label">Min</div></div>
            <div class="time-box"><div class="time-number" id="seconds">00</div><div class="time-label">Seg</div></div>
        </div>
    </div>

    <div class="section">
        <h2>Cuándo y Dónde</h2>
        <div class="card">
            <h3>El Gran Día</h3>
            <p><strong>19 de Junio de 2027</strong></p>
            <p>Hotel San Camillo</p>
            <p>Navarrete, La Rioja</p>
            <a href="https://maps.google.com" target="_blank" class="btn">Cómo llegar</a>
        </div>
    </div>

    <div class="section">
        <h2>Confirmación</h2>
        <p>Agradecemos que nos confirméis vuestra asistencia antes del 1 de mayo de 2027.</p>
        <a href="https://wa.me/34608XXXXXX?text=¡Hola!+Confirmamos+nuestra+asistencia+a+la+boda." target="_blank" class="btn">Confirmar Asistencia</a>
    </div>

    <div class="footer">
        <p>Un anillo para gobernarlos a todos. Una boda para reunirlos a todos.</p>
    </div>

    <script>
        // Configurar la fecha de la boda: 19 de junio de 2027
        const weddingDate = new Date("June 19, 2027 13:00:00").getTime();

        const timer = setInterval(function() {
            const now = new Date().getTime();
            const distance = weddingDate - now;

            const days = Math.floor(distance / (1000 * 60 * 60 * 24));
            const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
            const seconds = Math.floor((distance % (1000 * 60)) / 1000);

            document.getElementById("days").innerHTML = days;
            document.getElementById("hours").innerHTML = hours < 10 ? "0" + hours : hours;
            document.getElementById("minutes").innerHTML = minutes < 10 ? "0" + minutes : minutes;
            document.getElementById("seconds").innerHTML = seconds < 10 ? "0" + seconds : seconds;

            if (distance < 0) {
                clearInterval(timer);
                document.getElementById("countdown").innerHTML = "¡HOY ES EL GRAN DÍA!";
            }
        }, 1000);
    </script>
</body>
</html>
